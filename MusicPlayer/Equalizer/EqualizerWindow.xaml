<Window x:Class="MusicPlayer.Equalizer.EqualizerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicPlayer.Equalizer"
        mc:Ignorable="d"
        Title="EqualizerWindow" Height="300" Width="400"
        Style="{StaticResource {x:Type Window}}">
  <DockPanel>
    <Grid DockPanel.Dock="Top" Margin="8,4,8,4">
      <ComboBox x:Name="EqPresets" Text="Presets"/>
    </Grid>
    <Grid DockPanel.Dock="Bottom" Margin="8,4,8,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition/>
        <ColumnDefinition/>
      </Grid.ColumnDefinitions>
      <Button Grid.Column="2" Content="Reset" Click="Button_Reset"/>
    </Grid>
    <Grid Background="{StaticResource DarkInsetPanel}">
      <local:EqualizerControl/>
    </Grid>
  </DockPanel>
</Window>
